# Accounts API

Owns listener/artist registration, token login/logout, profiles, application settings,
password reset, account deletion, and user follow relationships. Django's built-in user
model is retained to avoid an unsafe mid-project AUTH_USER_MODEL migration.
