# Playlists API

Creates, renames, deletes, and populates playlists. Limits are read from the active
subscription plan: Basic 6, Silver 100, Gold unlimited. Tracks can be added, removed,
and reordered while ownership is enforced server-side.
