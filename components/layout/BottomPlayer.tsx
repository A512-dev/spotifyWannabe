import { PlayerShell } from "@/components/player/PlayerShell";

// Layout-level alias keeps pages independent from the player's implementation.
export function BottomPlayer() {
  return <PlayerShell />;
}
