server {
    listen 80;
    server_name _;
    server_tokens off;

    # Track uploads may be as large as 200 MiB in the Django validators.
    client_max_body_size 210m;
    client_body_timeout 120s;

    # Audio is served only through Django's signed playback/download endpoint.
    # Returning 404 here prevents bypassing subscription and daily-stream checks
    # with a guessed or previously leaked storage path.
    location ~ ^/media/music/tracks/[^/]+/audio/ {
        return 404;
    }

    location /media/ {
        alias /var/www/media/;
        autoindex off;
        add_header X-Content-Type-Options nosniff always;
    }

    location /static/ {
        alias /var/www/static/;
        autoindex off;
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
        add_header X-Content-Type-Options nosniff always;
    }

    location / {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_request_buffering off;
        proxy_connect_timeout 10s;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
